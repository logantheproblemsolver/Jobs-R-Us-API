CREATE TABLE jobs (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  company TEXT NOT NULL,
  salary_range TEXT DEFAULT 'Unknown',
  description TEXT NOT NULL,
  link TEXT NOT NULL
);
